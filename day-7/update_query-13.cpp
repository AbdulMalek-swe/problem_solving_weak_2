#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int q;
    cin >> q;
    while (q--)
    {
        int n,m;
        cin >> n >> m;
        string str1;
        cin >> s;
        set<int>s;
        for (int i = 0; i < m; i++){
            int x;
            s.insert(x);
        }
        string str2;
        cin >> str2;
        // sort string2 
        sort(str2.begin(), str2.end());
       for(auto it : s){
        cout<<it<<" ";
       }
        
    }
    return 0;
}
